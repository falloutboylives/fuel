/*
 ** FUEL extensions handling
 */

const fs = require("fs");

function callExtension(extension, global, params, req, res) {
  // TODO: Implement the callExtension function
  // "global" controls whether the extension is global or package-specific
}

module.exports.callExtension = callExtension;
